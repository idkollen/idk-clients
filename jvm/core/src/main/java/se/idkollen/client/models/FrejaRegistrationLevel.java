package se.idkollen.client.models;

/** Minimum required Freja eID registration level. */
public enum FrejaRegistrationLevel {
    /** Standard Freja eID registration (default). */
    EXTENDED,
    /** Enhanced Freja eID+ registration. */
    PLUS,
}
